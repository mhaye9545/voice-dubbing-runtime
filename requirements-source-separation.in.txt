# Isolated Demucs CPU worker for 8-15 second reference candidates (Python 3.11 x64).
# Keep this separate from .venv-cpu, .venv-xtts, and the FrameExtract app environment.
# Direct official CPU wheel URL prevents a CUDA or mutable-index Torch resolution.
demucs==4.1.0
numpy==1.26.4
psutil==7.2.2
sphn==0.2.1
torch @ https://download.pytorch.org/whl/cpu/torch-2.6.0%2Bcpu-cp311-cp311-win_amd64.whl
